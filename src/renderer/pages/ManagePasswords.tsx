import { Stack, Typography } from '@mui/material';
import React from 'react';

export function ManagePasswords(): React.ReactElement {
  return (
    <Stack height="100%" width="100%">
      <Typography>Manage Passwords</Typography>
    </Stack>
  );
}

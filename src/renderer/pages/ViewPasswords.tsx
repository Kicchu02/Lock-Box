import { Stack, Typography } from '@mui/material';
import React from 'react';

export function ViewPasswords(): React.ReactElement {
  return (
    <Stack height="100%" width="100%">
      <Typography>View Passwords</Typography>
    </Stack>
  );
}
